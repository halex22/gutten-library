// import BookService from "./services/books-service.js";
// import HtmlConstructor from "./services/html-service.js";

// const root = document.getElementById("root")
// const pippo = new BookService();
// const urlParams = new URLSearchParams(window.location.search);
// const urlId = parseInt(urlParams.get(`id`));

// function pippo2(book) {
//   const pippo4 = HtmlConstructor.createDetailedBookHtml(book)
//   root.appendChild(pippo4);
// }

// function getBook(id) {
//   pippo.fetchBookFromId(id).then(book => pippo2(book))
// }

// getBook(urlId);